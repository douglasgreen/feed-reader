{
  "name": "douglasgreen_feed-reader",
  "description": "RSS feed reader",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "lint": "npm run lint:js && npm run lint:md && prettier --check .",
    "lint:js": "eslint .",
    "lint:md": "markdownlint \"**/*.md\"",
    "format": "prettier --write . && markdownlint \"**/*.md\" --fix"
  },
  "devDependencies": {
    "@eslint/js": "^10.0",
    "eslint": "^10.7",
    "eslint-config-prettier": "^10.1",
    "eslint-plugin-security": "^4.0",
    "eslint-plugin-unicorn": "^71.1",
    "globals": "^17.7",
    "markdownlint-cli": "^0.49",
    "prettier": "^3.9",
    "prettier-plugin-sql": "^0.20"
  }
}
